<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RouteTask" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowSplashScreenBackground">@color/splashScreen_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/route_logo</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.RouteTask" parent="Base.Theme.RouteTask" />
</resources>